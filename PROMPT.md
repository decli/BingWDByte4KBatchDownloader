# 项目提示词

本文档记录生成本项目时使用的核心提示词，方便后续复盘、迭代或重新生成。

## 原始需求提示词

```text
我想请基于 https://bing.wdbyte.com/zh-cn/ 上的图片开发一个油猴脚本，实现如下功能：每当我打开 https://bing.wdbyte.com/zh-cn/ 列表页时在每张缩略图右小角设置一个复选框，我可以勾选选中它，支持多张图片多选，选中后，页面右侧悬浮出现一个批量下载的按钮（按钮可拖放位置），点击按钮可以选择保存目录，选择保存目录后，开始依次自动下载 4k（UHD） 图片保存到指定目录，保存时按如下规则重命名：比如名为 2026-05-19 Download 4k 的图片打开链接后地址为  https://cn.bing.com/th?id=OHR.SpainLighthouse_ZH-CN6024263415_UHD.jpg&rf=LaDigue_UHD.jpg&pid=hp&w=3840&h=2160&rs=1&c=4  ，那保存时的名字取 OHR. 开始 至第一个 & 符号之间的字符串，比如 SpainLighthouse_ZH-CN6024263415_UHD.jpg 就是这个链接的图片名称，如遇本地文件夹已存在同名文件，则跳过这张图片。下载完成后告诉用户你的下载目录是什么，一共下载了多少张，成功多少，失败多少，并把失败的 url 列出来供我复制验证。
ok 以上就是完整需求，请你评估是如果可以实现 就直接开干
```

## 调试补充提示词

```text
勾选2张图片后 点击 批量下载按钮没有反应
```

该反馈对应 `1.0.1` 中的拖拽点击修复：将悬浮按钮拖拽逻辑从 `setPointerCapture` 调整为文档级 `pointermove` / `pointerup` 监听，避免普通点击被拖拽逻辑吞掉。

## 当前实现目标

- 为 `https://bing.wdbyte.com/zh-cn/` 列表图片添加多选复选框。
- 选中后显示可拖动的批量下载按钮。
- 使用 File System Access API 选择保存目录。
- 使用 `GM_xmlhttpRequest` 依次下载选中 UHD 图片。
- 按 Bing 图片 URL 中的 `OHR.` 文件名保存。
- 已存在同名文件时跳过。
- 下载完成后展示统计信息和失败 URL。
